#include "main.h"
/**
 *print_numbers- prints 0 to 9 .
 */
void print_numbers(void)
{
	int c;

	for (c = '0'; c <= '9'; c++)
	{
		if (c != '4' && c != '2')
		_putchar(c);
		else
	}
	_putchar('\n');
}
