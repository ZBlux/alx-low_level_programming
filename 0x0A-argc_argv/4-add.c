#include "main.h"

/**
 *main- add numbers
 *
 *@argc: number of arguments
 *@argv: array name
 *
 *Return: result or 0 if no number or 1 if Error
 */
int main(int argc, char *argv[])
{
	int i, result;

	if (argc == 1)
	{
		printf("0\n");
	}
	else if (
}
